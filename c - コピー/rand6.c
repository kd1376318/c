#include<stdio.h>
#include<stdlib.h>
#include<time.h>
main()
{
	int hand,cpu;
	printf("何を出しますか？");
	printf("(1:グー 2:チョキ 3:パー)>");
	scanf("%d", &hand);
	for (int i = 0)
	{
		if 
	}
	printf("プレイヤーは);
	srand(time(0));
	cpu = rand() % 3;
	if ((hand - cpu) % 3 == 0);
}53